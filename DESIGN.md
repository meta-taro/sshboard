# DESIGN.md — sshboard

> **まだ何も決まっていません。**画面は 1 枚もありません（Phase 0）。
> ここは Phase 1 の 1-A（人が使う画面）に着手するときに、**実物を見ながら**埋めます。
> 先に埋めると、実装が「文書に合わせる作業」になります。

## 決まっていること（PRD / decisions 由来・変えない）

| | |
|---|---|
| **既定の画面** | **ファイル 2 ペイン**（ローカル / リモート）。ターミナルは**タブ**（D4） |
| **副ユーザー要件** | 既存の SFTP クライアント / ターミナルの**使い勝手から離れない**。端末を覚えさせない |
| **帯** | AI と人の操作が同じ 1 本に流れる。行頭に `[AI]` / `[Human]`（PRD §4-2） |
| **端末描画** | xterm.js。**ANSI の解釈を自前で書かない**（D7） |
| **フレームワーク** | SvelteKit + Tauri 2 |

## 未定（Phase 1 で埋める）

- カラーパレット
- タイポグラフィ
- 余白 / 角丸 / ボタン / カード
- 帯の見せ方（常時表示か、引き出しか）
- 接続先の表示（**スクリーンショットで伏せられる形にしておく**・PRD §8）
- 自前ヘッダーの中身（何を置くか・高さ・ボタンの見た目）。**自前にすること自体は決定済み**（D17）
- 端末の文字サイズ・フォント・配色を人が変えられるようにするか（`xterm.js` の
  `fontSize` / `fontFamily` / `theme` にそのまま乗る）
- **端末の日本語フォント。**`xterm.js` は日本語を 2 桁分確保するので、
  そこへ 1 桁幅のグリフが描かれると**文字の間が空く**（2026-08-26 に実機で確認）。
  日本語のグリフを持つ等幅フォントを候補に入れて緩和しているが、
  **半角と全角の比が正確に 1:2 のフォントを既定にしないと完全には揃わない。**
  対象が国内サーバーなので、ここは避けて通れない

## この製品に固有の注意

**画面そのものに接続先が写り込みます。**
ホスト名・プロンプト・パスが出る場所を設計段階で把握しておき、
デモやスクリーンショットで伏せられるようにします。**出す前しか対策できません。**

## 接続の印 — 色とタグ（dbboard から移植・**決定**）

一覧の 1 行に、**色**と**タグ**を付けられます。dbboard の `mark.rs` / ADR-0126 からの移植（D5）。

**色は「名前」で保存します。16 進数で保存しません。**

- **名前は、色が見えないときに使える。**読み上げ・口頭・メモ。`#1a73e8` はどれでもない
- 明るい行と暗い行で要る値が違う。**名前なら配色側が選べる。**
  16 進数を書くと、片方のテーマが設定ファイルに焼き付く
- **16 色。**dbboard は 8 色だが、こちらは対象サーバーが 13 台以上あり、
  **8 だと色がぶつかる**（実測・2026-08-27）。増やすほど 1 色ずつの区別は付きにくくなるので、
  **タグと併せて使う前提**

| | |
|---|---|
| 色（16・スペクトル順・2 行 × 8） | `red` `orange` `amber` `yellow` `lime` `green` `emerald` `teal` `cyan` `sky` `blue` `indigo` `violet` `purple` `magenta` `pink` |
| タグ | 自由文・**12 文字まで**（バイトではなく文字数） |

**タグは色の相棒で、白黒のスクリーンショットでも色覚特性のある人にも効く方**です。
色だけを印にしないのは、そのためです。

**「名前 → 実際の色」の対応表は仮置きです。**いまは
`apps/desktop/src/lib/components/ConnectionManager.svelte` の `--mark-*` にあります。
Phase 1-A で人が決め、明暗のテーマを足すときはそこだけ増やします。

## 多言語 — 11 言語（dbboard から移植・**決定**）

`en` `ja` `ko` `zh-CN` `zh-TW` `de` `fr` `es` `pt-BR` `ru` `it`。

- **既定は OS の言語。**選んだら残します
- **`en` が正本。**訳の無い鍵は `en` へ落ちます。
  **画面に鍵がそのまま出るよりはまし**だからです
- **検証の結果は「文言」ではなく「鍵」を返します。**文言を返すと、
  言語を切り替えても変わらない場所ができます
- **OS のメニューバーの訳も画面側に置きます。**Rust 側にも訳を置くと
  二重管理になり、**片方だけ直す**事故が起きます

## 配色 — 自動 / 明るい / 暗い（**決定**）

- **既定は自動**（OS に従う）。明示的に選んだときだけ `data-theme` を付ける
- **最初の描画より前に当てる**（`app.html`）。あとから当てると一瞬だけ違うテーマが見える
- 値は `apps/desktop/src/lib/styles/tokens.css` に集約。
  **コンポーネントに 16 進数を書かない**
- **端末だけは暗いまま。**ANSI の既定色は暗い背景に載る前提で作られている
- **印の色は彩度を落とさない。**16 色を見分けるには彩度が要る。
  色覚特性への配慮は**タグ**の側で担保している

## Phase 0 で置いた仮のもの（**決定ではありません**）

Phase 0 の 001 を実際に動かすために、見た目に関わるものを 2 つ置きました。
**どちらも人が決め直す前提の仮置きです。**AI が埋めた既定値をそのまま既成事実にしないため、
ここに明示しておきます。

| 仮置き | どこ | なぜ置いたか |
|---|---|---|
| アプリアイコン | `apps/desktop/src-tauri/icons/` | **これが無いとビルドが通らない。**単色の四角に横帯を 1 本引いただけのもの |
| 帯の画面 | `apps/desktop/src/routes/+page.svelte` | 001 の完了条件（帯に `[AI]` の行が出る）を目で確かめるための最低限。等幅・暗色の 1 画面のみ |

Phase 1 の 1-A に入るときに、**実物を見ながら**上の「未定」を埋め、この 2 つを差し替えます。
