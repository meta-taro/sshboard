# プロジェクトステータス — sshboard

- **現在フェーズ**: Phase 0（未知を潰す）**着手中**
- **最終更新**: 2026-08-26

## 完了した作業

**001 はまだ完了していません。**中核だけがヘッドレスで通った状態です。

| 出来たもの | 中身 |
|---|---|
| `crates/sshboard-band` | 人と AI の操作が流れる 1 本の帯。`[AI]` / `[Human]` の前置、購読、**受け取り待ち（ack）**。Tauri・MCP・SSH のどれにも依存しない |
| `crates/sshboard-mcp` | アプリ同居の MCP サーバー。ツールは `ping` 1 つだけ。**帯が受け取ってから応答を返す。**127.0.0.1 の Streamable HTTP（D15） |

**画面はまだ 1 枚もありません。**Tauri のアプリ本体は未着手です。

## テスト状況

**16 本。全部通っています。**

```
cargo test --workspace   →  16 passed; 0 failed
cargo fmt --all -- --check →  差分なし
cargo clippy --all-targets --all-features -- -D warnings → 警告なし
```

| どこ | 本数 | 何を見張っているか |
|---|---|---|
| `sshboard-band` | 9 | `[AI]` の前置・通し番号・全購読者の ack を待つ・詰まったら期限切れにする |
| `sshboard-mcp`（直接呼び） | 4 | `ping` が帯へ `[AI] ping` を載せる・**帯が受け取る前に応答を返さない**・画面が返さないときは失敗する |
| `sshboard-mcp`（HTTP 経由） | 3 | 生の JSON-RPC を HTTP へ投げて `ping` が通る・**ツールが `ping` 1 つだけ**（D3 の見張り）・**loopback にしか bind していない** |

HTTP のテストは rmcp のクライアント SDK を使っていません。SDK 同士で話せても、
別実装の MCP クライアントで動く保証にならないためです。

## 未完了の作業

| # | 未知 | 状態 |
|---|---|---|
| 001 | Tauri 2 に MCP を同居させ、GUI の帯へリアルタイムに出す | **着手中**（下記の残り 3 点） |
| 002 | 稼働中サーバーへ実際に SSH で繋ぐ | 未着手 |
| 003 | SSH ライブラリの選定（D6） | 未着手（002 の結果待ち） |
| 004 | 資格情報を OS ストア / ssh-agent から読む | 未着手 |
| 005 | `tail -f` を GUI と MCP へ同時に流す | 未着手 |

### 001 の残り

1. **Tauri 2 のアプリを作り、帯を画面に出す。**いまの帯は購読者がいないまま動いている
2. **画面が実際に描いてから ack を返す配線。**ここが繋がって初めて「先か同時」が実物になる
3. **Windows での確認。****未着手。**手元は macOS のみ

## 次のタスク

1. 001 の残り 3 点
2. 001 が通ったら 002。002 の結果で 003 が決まる

## 技術的決定

`.claude/decisions.md` を参照（D1〜D16）。

**未決は 2 つだけです。**

- **D6** — SSH ライブラリ（`russh` / `ssh2`）。Phase 0 の 003 で決める
- **D10** — 誰が実装するか

D15（MCP は loopback の Streamable HTTP）と D16（帯が受け取るまで応答を返さない）を
2026-08-26 に追加しました。

## 既知の問題

- **MCP の口に認証がありません。**loopback にしか bind していませんが、同じ端末の
  別プロセスからは叩けます。**Phase 1 で実際にサーバーを触るツールを載せる前に
  トークンを必須にすること**（D15）。Phase 0 の `ping` は何も触らないので今は無害
- **MCP のポート番号の扱いが未決。**いまは OS に空きを選ばせています。
  MCP クライアントへ登録する形が決まっていません（D15）
- **Phase 0 の 001 / 004 は Windows と macOS の両方で通す条件です。**
  手元は macOS のみ。**片 OS だけ通して完了にしない**
- **002 / 004 / 005 は実機のサーバーが要ります。**手元で代用できません
- CI はまだ個人情報チェックだけです。**format / lint / test を CI に足していません**

## 人にしかできない工程で、止まっているもの

- **Windows 実機での 001 / 004 の確認**（product-baseline §29）
- **push の最終確認**（commit は AI、push は人間）
