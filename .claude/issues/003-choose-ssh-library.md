# 003. SSH ライブラリを決める（Phase 0・D6 の決着）

候補は `russh`（純 Rust）と `ssh2`（libssh2 バインディング）。

## 判断の仕方

**カタログで決めません。**001 と 002 を実際に通せた方を採ります。

判断材料は 2 つだけ:

1. **実機に繋がるか**（古い鍵交換方式・暗号方式・002 の結果）
2. **`sftp` サブシステムの扱いやすさ**（GUI とストリームの両方から使う）

## 完了条件

- [ ] 両方で 002 を試した（片方だけ試して決めない）
- [ ] 採用した方と、その理由を `.claude/decisions.md` の D6 へ書き、**【決定】に変えた**
- [ ] 採らなかった方について「どこで落ちたか」を 1 行残した

## やらないこと

- 抽象化レイヤを挟んで両対応にしない。**1 つ選ぶ**
