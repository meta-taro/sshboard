# 005. `tail -f` を GUI と MCP へ同時に流す／ANSI とプレーンを分ける（Phase 0）

## なぜ

障害調査の主作業はログの追尾です。
ここで **GUI 側は色付きで見え、MCP 側はプレーンなテキストで返る**必要があります。

MCP へ ANSI エスケープが混ざったまま返すと、AI が読む文字列にゴミが入ります。
GUI から色を落とすと人が読みにくくなります。**同じ 1 本の出力を、面ごとに違う形で出す。**

## やること

- 実機で `tail -f` 相当を流す
- GUI（xterm.js）へは ANSI のまま描画する
- MCP へはプレーンなテキストで返す
- **止められること**を確認する（人が AI の実行を止め、同じセッションで続けられる・PRD §4-3）

## 完了条件

- [ ] 同じ 1 本の出力が、GUI と MCP の両方へ同時に出た
- [ ] MCP 側の文字列に ANSI エスケープが 1 つも混ざっていない
- [ ] GUI 側は色が保たれている
- [ ] 流している途中で人が止められ、止めたあと人が同じセッションで続けられた

## やらないこと

- ANSI の解釈を自前で書かない（xterm.js に任せる・D7）

---

## 実行結果（2026-08-26・macOS・**実機なし**）

**チェックボックスは埋めていません。**合否は実物の画面を見た人が付けてください（product-baseline §19）。

### 何を動かしたか

**サーバーへは繋いでいません。**002 が通っていないためです。
代わりに、アプリの中で**合成の出力**を流し、面ごとの分岐だけを確かめました。

- 出力元は 1 つ（`OutputStream`）。**GUI 用にもう 1 本作っていません**（PRD §4-1）
- GUI へは `stream://raw` で**生のまま**渡し、xterm.js が描く（D7）
- MCP は `read_stream` で**素のテキスト**を受け取る

### 出た値

`sh tools/check-005.sh <MCP の URL>` の結果:

```
{"jsonrpc":"2.0","id":2,"result":{"content":[{"type":"text","text":"2026-08-26 19:40:01 INFO  service started
2026-08-26 19:40:02 INFO  listening
2026-08-26 19:40:05 WARN  queue is filling up
2026-08-26 19:40:07 INFO  設定ファイルを読み込みました
2026-08-26 19:40:09 ERROR disk usage 92%
2026-08-26 19:40:11 ERROR no space left
"}],"isError":false}}

OK: MCP 側の文字列に ANSI エスケープはありません
```

流した元のバイト列には、色（`ESC [ 31 m`）・端末タイトルの変更（`ESC ] 0 ; … BEL`）・
`CRLF`・日本語が入っています。**MCP 側にはどれも残っていません。**日本語も壊れていません。

### 確かめ方（人の手が要ります）

1. アプリを起動する
2. 画面の **「Phase 0 の確認用に流す」** を押す
3. `sh tools/check-005.sh http://127.0.0.1:<ポート>/mcp`

ポートは起動した端末の `MCP listening on ...` に出ます。
`SSHBOARD_PHASE0_DEMO=1` を付けて起動すると、ボタンを押さずに流れます。

### まだ埋まっていないもの

- **実機の `tail -f`。**合成の出力で通っただけです。002 が通ってから差し替えます
- **GUI 側に色が残っているかの目視。**xterm.js が描いていますが、
  **色が保たれているかは人が見て判断してください**
- **止めて、人が同じセッションで続けられること。**
  `stop()` で止まることは自動テストで見ていますが、
  「止めたあと人が続けられる」は SSH セッションの話なので 002 のあとです
