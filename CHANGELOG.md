# 変更履歴

> **この形式は機械が読みます。**MCP の `about_sshboard` がこのファイルを
> **バイナリへ焼き込んで**返します（別ファイルを読みに行くと、配った版と中身がずれます）。
>
> **版の見出しは `## <バージョン> — <一行>`。**この形を崩すと、AI 側に届かなくなります。
> 新しい版を上に足してください。

## 0.1.12 — root が要る操作へ届く道を開いた

- **`become = "ask"`**（D48 / Issue #19）。接続ごとに権限の上げ方を書けるようにした。
  `ask` は承認の画面でパスワードを聞き、**標準入力から `sudo -S` へ渡す。**
  **保存しない。コマンド行にも画面にも記録にも出さない。1 回の許可で 1 回だけ。**
- **`become = "sudoers"`** は `sudo -n` を付ける（入力待ちで固まらない）。
  書かなければ「上げない」で、いままでと 1 文字も変わらない。
- **許可は 5 分で切れる。**押したまま忘れられた札が、パスワードを抱えて残らない。
- **`max_per_hour` が一度も効いていなかった**（D45 から在った欠陥）。
  数える入れ物が許可の札と同じで、走った回数が 1 件も残っていなかった。直した。
- **`/etc/shadow` / `/etc/gshadow`** を、`operations.toml` に書いても弾く一覧へ追加。
- **「まだサーバーに繋がっていません」を 2 か所が返していた**（Issue #8）。
  宛先が決まっていない場合と、押したタブが開いていない場合を分けた。
  **どちらも［ログ］に 1 行残る。**「接続を開いてください」は、
  すでに開いている人には効かない案内だった。

## 0.1.11 — 状態を変える操作を、人の承認つきで走らせられるようにした

- **`operations.toml`**（D45 / Issue #17）。人が列挙した操作を、AI は **id だけ**で呼べる。
  1 回目は必ず断られ、**画面に「何が走るのか」がそのまま出る。**
- **用途別ツールが打ったコマンドと出力が、端末の面に出るようになった**（D41 / Issue #14）。
  それまで AI がサーバーで何を見たのか、人は追えなかった。
- **`show_view`**（D44 / Issue #15）。AI が人の画面を「こちらを見て」と動かせる。
  **奪えない** —— 人が問いに答えている最中・入力中・直前に自分で切り替えた直後は動かない。
- **`pending_status`**（Issue #20）。人が答えたかを AI が問い合わせられる。
- **握りを渡すと別のシェルが開いていた**（Issue #21）。人が `su -` した状態が引き継がれなかった。
- 端末の色が繋がっておらず、**読めない色が 5 つ混ざっていた**（Issue #18）。
- Windows で Ctrl+V が効かなかった。
- ［操作の記録］タブのレイアウトが崩れていた（Issue #22）。

## 0.1.10 — AI が打った内容が画面に出ていなかった

- **AI が端末で打った内容が［端末］タブに出ていなかった**（Issue #14）。
  承認の画面は「画面にそのまま出ます」と言っており、**実態と食い違っていた。**
- 許可ダイアログのアイコンが豆腐（□）になっていた（Issue #16）。
- 端末タブを行き来すると、それまでの表示が消えていた（Issue #11）。

## 0.1.9 — 出せない画面を案内していた

- **`connect` が「画面で入れてください」と返るのに、画面に何も出なかった**（Issue #13）。
  AI が頼めば、人の画面にパスフレーズの問いが出るようにした。
  **その失敗が記録にも残っていなかった。**

## 0.1.8 — AI が人から端末を奪えていた

- **`console_stop` が裏口だった。**AI が人の握りを外せた（D42）。
- **AI が端末を握るには、人の許可が要る**（D42）。頼んだことは記録に残り、画面に出る。
- **部品を描いて確かめるテストを入れた**（D43 / Issue #10 の落とし前）。
  型検査は 283 files 0 errors のまま、端末が 3 日間 1 バイトも表示していなかった。
- 端末で右クリック貼り付け。端末の面を開いたら端末も開く。

## 0.1.7 — 端末が 3 日間、1 バイトも表示していなかった

- **端末の面に出力が 1 バイトも届いていなかった**（Issue #10）。
  シェルは生きていて、`read_stream` からは全部見えていた。**画面だけが繋がっていなかった。**
- `connect` の説明が実装と食い違っていた（Issue #9）。**7 日間そのままだった。**
- 接続状態の購読を、窓と同じ寿命の所へ移した（D40 / Issue #8 の一部）。
  それまでファイルの面の部品が持っており、**別のタブに居る間は更新が止まっていた。**

## 0.1.6 — 接続を並べ替えられるようにした

- 接続の並べ替え（D37）。掴む所（⠿）と開く所を分けた。tab → ↑↓ でも動く。
- **stdio 中継**（D38 / Issue #2）。**合言葉をどこにも書かせない。**
- 更新の札が「再起動で何が切れるか」を名乗るようにした（D34 追記 3）。
- 端末の面からも繋げるようにした（D39）。画面の案内が行き止まりだった。

## 0.1.5 — 繋ぎ方を 1 つ選ぶ形にした

- **鍵とパスワードの欄が同時に出ていた**（Issue #7）。
  **鍵で繋ぐ人が、使われないパスワードを資格情報ストアへ預けてしまう**事故が実際に起きた。
- **パスフレーズは繋ぐときに正面から聞く。保存しない。**
- ファイル画面が古い状態を出したままにしないようにした（Issue #8 の一部）。

## 0.1.4 — 認証の道が 3 本になった

- **パスワード認証を入れた**（Issue #4）。0.1.3 までは ssh-agent と鍵ファイルだけで、
  **素の Windows では ssh-agent が既定で無効**なため、実質つながる道がほとんど無かった。
- `update_connection` を入れた（Issue #5）。**指紋・ホスト・利用者・ポートには触れない。**
- バンドルがログインのパスワードを運んでいなかった。
- 「sshboard について」を作った。

## 0.1.3 — 画面を壊していた 3 つを直した

- 実機のキャプチャで見つけた崩れを 3 つ。
- 取り込みが「押しても何も起きない」状態だった。
- 暗いテーマでスクロールバーが白いままだった（Windows 実機）。
- 画面を撮るのを **OS ではなく画面側**へ移した（D36）。

## 0.1.2 — 接続情報を持ち出せるようにした

- **接続情報の書き出し／取り込み**（D18）。暗号化して 1 ファイルにする。
  **途中で失敗したら戻す。**
- **2 つ目の起動を許さない**（D35 / Issue #2）。
- 戻る／進む・マウスの側面ボタン・キーボード・左右の同期移動。

## 0.1.1 — Windows 実機で試せる形にした

- **自動更新**（D34）。**ただし黙って入れ替えない。**
- **MCP のポートを 22022 に固定**（D33 / Issue #2）。毎起動変わって登録し直しになっていた。
- タイトルバーを自前にした（D17）。
- バージョンを 3 か所まとめて上げる道具と、**ずれたら落ちるテスト。**

## 0.1.0 — 最初の α

- 人と AI が **1 本の SSH を共有する**形（PRD §4-1）。GUI も MCP も同じ実行体を通る。
- ファイルの上げ下げ、用途別の読み取りツール、人と AI が共有する端末（D29）。
- **`run_command(cmd)` 相当を作らない**（D3）。AI が渡せるのは、人が書いた一覧の識別子だけ。
- **AI の書き込みは、接続ごとに人が列挙したディレクトリの下だけ**（D22）。**既定は空。**
