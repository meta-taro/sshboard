# sshboard — PRD

> 初版 2026-08-25。**方向性の正本です。**進め方は `.claude/roadmap.md`、決定の理由は `.claude/decisions.md`。

---

## 0. 一行で

**リモートサーバーの中身を、人と AI が同じ画面で見る道具。**

ファイルも、コマンドの出力も、同じ 1 本の SSH の上で見ます。

---

## 0-1. なぜ作るか

従来型のレンタルサーバー / VPS の上で動き続けているサービスがあります。
そこを AI と一緒に保守すると、毎回これが起きます。

```
人 : このサービスの設定を直したい
AI : サーバー側の状態が分からないので教えてください
人 : （ターミナルを開いて調べて、貼る）
AI : ではこの設定ファイルの中身も見せてください
人 : （SFTP を開いて落として、貼る）
```

**この往復を消すための道具です。**
AI にサーバーを操作させる必要はありません。**見せるだけで消えます。**

---

## 0-2. 具体的な用途

| 用途 | 必要なもの | Phase 1 で足りるか |
|---|---|---|
| **障害時のサーバ調査** | ログ（ファイル）＋ ディスク・プロセス・サービス（コマンド） | 足りる |
| **メールサーバの設定調査** | 設定ファイル（ファイル）＋ `postconf -n` 相当・listen ポート（コマンド） | 足りる |
| **改善の提案** | 上を読んだ結果 | **機能を作らない**（下記） |
| **成果物をサーバーへ置く** | ファイルを上げる（ディレクトリ作成込み） | 足りる（**D22 で入れた**） |

### 「提案」のための機能は作りません

読めれば、提案は AI が自分で書きます。**アプリ側に足すものはありません。**
レポート生成機能を内蔵すると、AI チャット UI を内蔵しない方針（§5）と衝突します。

**上の 3 用途はすべて読み取りです。**当初はそれを根拠に Phase 1 を読み取り専用にしました（D2）。

**それは用途を数え落としていました。**運用者の実際の仕事は「VPS へプロダクトを上げる」で、
読み取りだけでは 1 ミリも楽になりません。**D22 で書き込みを入れました。**
ただし **AI が書ける場所は、接続ごとに人が決めた囲いの中だけ**です（既定は空 ＝ 書けない）。

---

## 0-3. 用途を近代化しません

**古い用途を近代化します。用途そのものを無理に近代化しません。**

- サーバーを別の基盤へ移させる道具ではありません
- 従来の構成のまま使えることが価値です
- **従来型のサービスは今後 10 年以上残ります。**そこに AI 開発を持ち込む道具が存在していません

---

## 0-4. Windows 専用ツールを Mac で使いたい層がいる

**WinSCP は Windows 専用です。**「Mac で WinSCP が使いたい」は古くからある需要で、
FFFTP・Tera Term・Poderosa も同じ形（Windows 側に資産があり、Mac に持っていけない）です。

sshboard は Tauri で **Windows / macOS の両方に出ます。**
**AI を使わない人にとっても、ここだけで移ってくる理由になります。**

そして**その層は、鍵を `.ppk` で持っています**（実測・Issue 002）。
`.ppk` を読めないと入口で詰まるので、そこは案内します（D19）。

**これは副産物ではなく、打ち出す価値です。**AI を使わない人が普通の
SFTP クライアント / ターミナルとして使い、必要になったときだけ AI を足せる、
という順序で入ってこられます。

## 1. 一次ユーザー

| | |
|---|---|
| **主** | 既存サービスを AI と一緒に保守する開発者 |
| **副** | 従来の慣習でないと作業できない担当者。**既存の SFTP クライアント / ターミナルの使い勝手から離れない**ことが要件 |

副ユーザーのために、**既定の画面はファイル 2 ペインです。**ターミナルはタブで開きます。
**副ユーザーに端末を覚えさせません。**

---

## 2. 成功条件（唯一）

**AI が「サーバー側の状態を教えてください」と言わなくなること。**

ダウンロード数でも star 数でもありません。**実際の保守作業で往復が消えたかどうか**です。

---

## 3. Phase 1 の範囲 — 読み取りだけ

**書き込みを一切入れません。**（D2）

### AI が呼べるもの

**実装済みのものに ✅ を付けています。**

| MCP ツール | 面 | 中身 | |
|---|---|---|---|
| `connect` / `disconnect` | 共通 | 登録済みの接続を開く / 閉じる。**同時に 1 本だけ** | ✅ |
| `session_status` | 共通 | いま何が開いているか・AI が書ける場所 | ✅ |
| `make_directory` | ファイル | ディレクトリを（親ごと）作る。**囲いの中だけ**（D22） | ✅ |
| `upload_file` / `write_file` | ファイル | 手元のファイル / その場の中身を上げる。**囲いの中だけ**（D22） | ✅ |

| MCP ツール | 面 | 中身 |
|---|---|---|
| `list_connections` ✅ | 共通 | 登録済み接続の一覧（**認証情報は返さない**） |
| `list_directory` ✅ | ファイル | ディレクトリ一覧 |
| `stat` ✅ | ファイル | 1 件の属性（サイズ・**8 進数の権限**・更新日時・所有者の番号） |
| `read_file` ✅ | ファイル | 中身をテキストで返す（設定ファイル・ログ） |
| `search` ✅ | ファイル | ファイル名 / 中身の検索。**深さと件数を切る**（切らないと返らない） |
| `download` | ファイル | ローカルへ取得。**MCP には置かない**（D27）。人は GUI から落とせる |
| `disk_usage` ✅ | サーバー | `df -h -P` |
| `process_list` ✅ | サーバー | プロセス一覧（`ps aux`） |
| `service_status` ✅ | サーバー | サービスの起動状態。**ユニット名は囲う**・`--no-pager` |
| `runtime_versions` ✅ | サーバー | OS と言語の版。**入っていないものは黙って出さない**（異常ではない） |
| `read_log` ✅ | サーバー | 指定ログの末尾（`tail`）。**パスは囲う** |
| `network_listen` ✅ | サーバー | listen しているポート（`ss`・無ければ `netstat`） |
| `run_readonly` ✅ | サーバー | **許可リストに載ったコマンドのみ**。渡せるのは識別子だけ |
| `list_readonly_commands` ✅ | サーバー | 人が許したコマンドの一覧。**既定は空**（触りません） |

### `run_readonly` が守れること・守れないこと

**守れること:** AI は**コマンド文字列を組み立てられません。**渡せるのは
人が `readonly.toml` に書いた項目の識別子だけで、引数のスロットもありません。
一覧に無いものは、**サーバーへ届く前に**断ります。

**守れないこと:** **製品は「そのコマンドが本当に読み取り専用か」を検証できません。**
`uptime` と `rm -rf /` を機械が見分ける方法はありません。走るのは、人が書いた文字列
そのものです。**安全を決めているのは、この仕組みではなく、書いた人**です。

**既定は空です**（D3 追記）。実務で何が要るかの一覧を誰も持っていないので、
製品側は 1 本も用意しません。断った分は帯へ出て `readonly-refused.log` に残り、
**足す判断は人**がします。

### 用途別ツールの引数は、必ず囲う

`service_status` と `read_log` は引数を取ります。**ここが `run_readonly` で
作らなかった注入面そのもの**なので、扱いを決めてあります。

- **コマンドを組み立てるのは製品側**（`sshboard-engine` の `probes`）。AI は値だけ渡す
- **引数は例外なく単引用符で囲う**（`sshboard-ssh` の `quote`）。
  「英数字だけなら素通し」という近道を作らない。**判定を 1 つ間違えたら終わり**で、
  間違いは「動いてしまった」形で現れる
- `--` を置いて、**`-` で始まる値がオプションとして読まれない**ようにする
- **返ってこないコマンドを作らない。**`systemctl status` は既定でページャへ流すので、
  端末の無い `exec` では返らない（`--no-pager` が要る）
- **書き込む語が混ざっていないことを、テストで見張る**（`tests/probes.rs`）

### なぜ「任意コマンド 1 個」にしないか

`run_command(cmd)` を 1 つ置けば全部できます。**それをやりません。**

- 任意コマンドを渡した時点で、**破壊的操作が「使わない約束」でしか防げなくなる**
- **約束は手順書であって、ゲートではありません**
- 許可リスト方式なら、**危険なコマンドは呼びようがありません**

許可リストへ足す操作は人が行い、設定ファイルの差分として記録に残ります。

### 人（GUI）の側は制限しない

**人は普通の SFTP クライアント / ターミナルとして自由に使えます。**
制限するのは AI の口だけです。

### 書き込みは、囲いの中だけ（D22）

**AI が書ける場所を、接続ごとに人が列挙します。既定は空 ＝ AI は 1 バイトも書けません。**

危険を「使わない約束」で防ぐのは手順書であってゲートではありません。
**書ける場所そのものを列挙**すれば、囲いの外は**呼びようがありません。**
これは任意コマンドを作らない理由（上）と同じ考え方です。

囲いを設定するのは**画面を見ている人だけ**で、**AI が登録した接続の許可は常に空**です。
自分で登録して自分に許可を出せるなら、囲いは意味を持ちません。

### Phase 2 へ回すもの

上書き以外の書き込み — 削除・リネーム・移動・パーミッション変更・転送キュー・
サービス再起動・パッケージ操作・任意コマンド実行・承認 UI・sudo の扱い

**MCP のツール一覧に、これらが生えていないことをテストで見張っています。**

---

## 4. 中核の設計思想

### 4-1. GUI と MCP は同じ実行体を共有する

```
   ┌──────────┬──────────┐        ┌──────────┐
   │ ファイル  │  端末     │        │   MCP    │
   │  2 ペイン │  タブ     │        │          │
   └────┬─────┴────┬─────┘        └────┬─────┘
        └──────────┴───────┬───────────┘
                           ▼
              ┌────────────────────────┐
              │  Operation Engine      │  ← 実装はここに 1 つだけ
              └───────────┬────────────┘
                          ▼
                       SSH 1 本
                    （sftp / exec）
```

**SFTP の実装を 2 つ持ちません。裏で見えない SSH セッションを張りません。**
見えないことが最大の危険だからです。

### 4-2. 誰が触ったかが画面に出る

```
[Human] $ cd /var/www
[AI]    $ df -h
Filesystem      Size  Used Avail Use% Mounted on
/dev/vda1        50G   38G   10G  80% /
[AI]    read /etc/postfix/main.cf  (4.1 KB)
```

ファイル面の操作も同じ帯に流します。**面が違っても記録は 1 本です。**

### 4-3. 人がいつでも割り込める

- AI の実行を止められる
- 止めた後、人が同じセッションで続きをやれる

---

## 5. やらないこと（明示）

| やらない | 理由 |
|---|---|
| **AI に書き込みを渡さない（Phase 1）** | 用途がすべて読み取りだから。危険がほぼゼロになり、本番サーバーを初日から対象にできる |
| **AI に任意コマンドを渡さない** | 許可リストで構造的に防ぐ（§3） |
| **AI に sudo を渡さない** | Phase 1 で権限昇格を扱わない |
| **自前の鍵ストアを作らない** | OS 資格情報ストア / ssh-agent へ委譲 |
| **AI チャット UI を内蔵しない** | エージェントはアプリの外にいる。提案文は AI が外で書く |
| **レポート生成機能を作らない** | 同上。読めれば書けるので、足すものが無い |
| **サーバーの移行を促さない** | 従来構成のまま使える道具であることが価値 |

---

## 6. Phase 0 — 未知を先に潰す

**5 つ。ここが通らないなら作りません。**

| # | 未知 | 潰し方 | 優先 |
|---|---|---|---|
| 1 | **MCP からの操作が GUI へリアルタイムで反映されるか** | Tauri 2 に MCP を同居させ、MCP 呼び出しが GUI の帯に出るところまで | **最優先** |
| 2 | 実機の SSH に繋がるか（古い鍵交換方式・暗号方式） | 稼働中サーバーへ実際に接続する | 高 |
| 3 | SSH ライブラリの選定（D6） | 実機で 1・2 を通せた方を採る | 高 |
| 4 | 資格情報の読み取り（OS ストア / ssh-agent） | Windows / macOS 両方で読む | 中 |
| 5 | 対話的出力（`tail -f`）を GUI と MCP へ同時に流せるか／ANSI とプレーンの分離 | 実機で流し、GUI は色付き・MCP はプレーンで返す | 中 |

**#1 が成立しないなら、この製品は成立しません。**最初にここを見ます。

---

## 6-1. MCP の口は合言葉で守る（D23）

`127.0.0.1` にしか bind しませんが、**同じ端末の別プロセスからは叩けます。**
読むだけの頃は実害が小さかったものの、**いまは書き込みが載っています。**

`Authorization: Bearer <token>` が合わないリクエストは、**initialize すら通しません。**
合言葉は接続一覧の隣に `0600` で置き、画面から 1 行で写せます。

---

## 7. 配布

- コード署名は初手では入れない（D12）。
  **ただし macOS のキーチェーン ACL はバイナリに紐づくため、未署名だと
  資格情報ストアを触るたびに承認が出ます**（実測・D23）。署名を入れる根拠がここにあります
- GitHub Releases でバイナリ配布
- **MCP サーバーを同梱する。**別ビルドを要求しません

---

## 8. 公開時の制約

public リポです。組織内の呼び名・役職名・実在の個人名・個人メール・他案件名・
**接続先ホスト名 / IP / ユーザー名**を成果物へ書きません。**commit history にも残しません。**

**ターミナルとファイル一覧は、画面そのものに接続先が写り込みます。**
README やデモ GIF を作るときは、ホスト名・プロンプト・パスを必ず伏せます。
GitHub の編集履歴は残るため、**出す前しか対策できません。**

### 発見される語をどこに置くか

製品名は `sshboard`（ブランド）です。**世界の開発者が探す語は名前の外に置きます。**

| 置き場所 | 入れる語 |
|---|---|
| リポジトリの description | MCP SFTP client / MCP SSH terminal / AI agent |
| GitHub topics | `mcp` `mcp-server` `sftp` `ssh` `ai-agent` `tauri` `rust` |
| README の 1 行目と最初の段落 | 同上（Google に拾われる） |
| MCP レジストリ / 一覧サイトの登録 | 同上（**実際に探される主経路はここ**） |

---

## 9. 未決

- D10 誰が実装するか
- MCP の URL の固定（ポートが起動ごとに変わるため、`claude mcp add` をやり直すことになる・D15）
