{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "画面が使う最小の権限。使うものだけ足す（PRD §21・product-baseline §21）。",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:event:default",
    "dialog:allow-open"
  ]
}
